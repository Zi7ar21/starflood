#pragma once

// Warning: Changing to double may cause memory leak, the extra precision can cause the tree to subdivide forever for some reason
#define STARFLOOD_REAL_TYPE float

#define STARFLOOD_VERBOSITY 3
